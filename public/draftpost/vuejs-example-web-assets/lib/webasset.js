var WebAsset = (function(){

    var api = function(opt){
        var asset = {
            secs: 0,
            secsPerTick: 10,
            postCount: 100,
            avgWordsPerPost: 800
        };
        return asset;
    };

    return api;

}());